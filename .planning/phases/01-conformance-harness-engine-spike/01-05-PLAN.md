---
phase: 01-conformance-harness-engine-spike
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - spike/playa_decode_probe.py
  - tests/test_playa_decode_probe.py
  - .planning/phases/01-conformance-harness-engine-spike/PLAYA-DECISION.md
autonomous: true
requirements: [ENG-04]

must_haves:
  truths:
    - "playa-pdf (or its pdfminer.six fallback) is proven to decode encodings and glyph geometry on at least 4 real documents including one Type0/Identity-H and one subset-font document, and the decision is recorded in Phase 1, not deferred"
    - "playa-pdf decode calls are confined to a single module with no abstraction layer built around them"
  artifacts:
    - path: "spike/playa_decode_probe.py"
      provides: "The one module boundary for playa-pdf (and pdfminer.six fallback) decode calls"
    - path: ".planning/phases/01-conformance-harness-engine-spike/PLAYA-DECISION.md"
      provides: "The recorded playa-pdf-vs-pdfminer.six decision with per-file evidence"
  key_links:
    - from: "spike/playa_decode_probe.py"
      to: "corpus/manifest.json"
      via: "selects >=4 real fixtures including type0_identity_h and subset_fonts categories"
      pattern: "manifest"
---

<objective>
Prove — or disprove — that `playa-pdf` decodes encodings and glyph geometry correctly on real
documents, confined to a single module, and record the decision (playa-pdf or its `pdfminer.six`
fallback) this phase, not later.

Purpose: ENG-04 and Risk #3 (research/SUMMARY.md) both flag `playa-pdf` as the least-corroborated
choice on the critical path. This is a throwaway spike per CONTEXT.md's discretion note — its code
does not need to survive into Phase 2, but its finding must be recorded now so Phase 2 does not
re-derive the same evaluation.

Output: `spike/playa_decode_probe.py`, `PLAYA-DECISION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-conformance-harness-engine-spike/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/SUMMARY.md
@corpus/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confine playa-pdf decode calls to one module; validate against >=4 real documents</name>
  <files>spike/playa_decode_probe.py, tests/test_playa_decode_probe.py</files>
  <read_first>
    .planning/research/STACK.md (playa-pdf capability table, "Alternatives Considered" pdfminer.six row)
    .planning/research/SUMMARY.md (Risk #3)
    corpus/manifest.json (pick >=4 files; must include at least one tagged `type0_identity_h` and one tagged `subset_fonts`)
    .planning/phases/01-conformance-harness-engine-spike/01-CONTEXT.md (code_context note: no abstraction layer, one module boundary IS the mechanism)
  </read_first>
  <action>
    Write `spike/playa_decode_probe.py` as the ONLY file in the repository that imports `playa` (or
    `pdfminer.six` for the fallback path) — do not build an interface, protocol, or base class
    around this; the single module file is the entire mechanism per CONTEXT.md's discretion note.
    Select at least 4 files from `corpus/manifest.json`, including at least one tagged
    `type0_identity_h` and one tagged `subset_fonts`. For each: open with `playa-pdf`, iterate
    pages, extract per-glyph records (code, glyph/CID, unicode if available, bbox/origin, advance,
    font reference) — the exact read-side fields research/ARCHITECTURE.md's run record needs.
    Record per file: decode succeeded without exception (yes/no), glyph count extracted, whether
    the Type0/Identity-H document's CIDs resolved to sane glyphs (spot-check against a known
    visible string in the document), whether the subset-font document's `/Differences` encoding
    resolved to sane glyph names (not garbage/empty). If `playa-pdf` fails or produces obviously
    wrong output on any of the selected files (e.g. zero glyphs on a document with visible text),
    re-run the same probe against `pdfminer.six` for that file behind a `--engine playa|pdfminer`
    CLI flag in the SAME file (still no abstraction layer — just two code paths in one module per
    the discretion note), and record the comparison. Write `tests/test_playa_decode_probe.py`
    asserting the probe runs successfully (no unhandled exception) against each of the >=4
    selected fixtures and that glyph counts are `> 0` for each.
  </action>
  <acceptance_criteria>
    `python spike/playa_decode_probe.py --manifest corpus/manifest.json --min-files 4` exits 0 and
    prints a per-file summary table including at least one `type0_identity_h` and one
    `subset_fonts` result; `grep -rl "^import playa\|^from playa\|^import pdfminer\|^from
    pdfminer" --include="*.py" . | grep -v spike/playa_decode_probe.py` returns empty (no other
    file imports either library); `pytest tests/test_playa_decode_probe.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_playa_decode_probe.py -q && python spike/playa_decode_probe.py --manifest corpus/manifest.json --min-files 4 && test -z "$(grep -rl '^import playa\|^from playa\|^import pdfminer\|^from pdfminer' --include='*.py' . | grep -v spike/playa_decode_probe.py)"</automated>
  </verify>
  <done>playa-pdf validated (or found wanting, with pdfminer.six comparison) against >=4 real documents including the two required categories, decode calls confined to one module, no abstraction layer built.</done>
</task>

<task type="auto">
  <name>Task 2: Record the playa-pdf vs pdfminer.six decision</name>
  <files>.planning/phases/01-conformance-harness-engine-spike/PLAYA-DECISION.md, pyproject.toml</files>
  <read_first>spike/playa_decode_probe.py output/summary from Task 1</read_first>
  <action>
    Write `PLAYA-DECISION.md` stating the decision unambiguously: "playa-pdf" or "pdfminer.six" as
    the primary library Phase 2 will build the content-stream interpreter's read side on, citing
    Task 1's per-file evidence table directly (copy or reference the exact glyph counts and
    pass/fail results). If `pdfminer.six` is chosen instead of `playa-pdf`, update `pyproject.toml`
    to add it as the actual runtime dependency (`uv add pdfminer.six`) and note whether `playa-pdf`
    is removed or kept installed for comparison purposes — state the choice explicitly, do not
    leave it ambiguous. This decision must be recorded in this phase per ENG-04's explicit wording
    ("or the switch to pdfminer.six is made and recorded in this phase, not later").
  </action>
  <acceptance_criteria>
    `PLAYA-DECISION.md` exists, states one library as the primary/default choice in a single
    unambiguous sentence, cites the Task 1 evidence table, and is dated 2026-08-12 or later; if
    the decision is `pdfminer.six`, `pyproject.toml` reflects it as an actual dependency.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-conformance-harness-engine-spike/PLAYA-DECISION.md && grep -qi "decision" .planning/phases/01-conformance-harness-engine-spike/PLAYA-DECISION.md</automated>
  </verify>
  <done>Decision recorded in this phase, not deferred to Phase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local/CI process ↔ real (including malformed) corpus PDFs, parsed by playa-pdf/pdfminer.six | Read-side decode probe over untrusted-shaped input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-01 | Denial of Service | A pathological corpus file hangs the decode probe | mitigate | Wrap each file's probe call with a wall-clock timeout; report timeout as a per-file failure rather than hanging the run |
| T-05-02 | Tampering (supply chain / package legitimacy) | `playa-pdf` is explicitly flagged MEDIUM-HIGH confidence with thin third-party corroboration by research (Risk #3) | mitigate | This task IS the mitigation — empirical validation against >=4 real documents before Phase 2 commits to it; `pdfminer.six` fallback is HIGH-confidence, research-corroborated, and available as the same module's alternate code path with zero architectural cost |
</threat_model>

<verification>
`pytest tests/test_playa_decode_probe.py -q` passes. Probe runs cleanly against >=4 real
documents including the two required categories. `PLAYA-DECISION.md` records an unambiguous
choice.
</verification>

<success_criteria>
ENG-04 and Risk #3 are retired: the playa-pdf bet is settled empirically, confined to one module,
and the decision is written down in Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-conformance-harness-engine-spike/01-05-SUMMARY.md` when done
</output>
