---
phase: 01-conformance-harness-engine-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md
  - pyproject.toml
  - uv.lock
  - tools/license_gate.py
  - tests/test_license_gate.py
  - tests/fixtures/agpl_gate_proof_red.txt
  - tests/fixtures/agpl_gate_proof_green.txt
  - .github/workflows/license-gate.yml
autonomous: true
requirements: [ENG-06, ENG-07]

must_haves:
  truths:
    - "A one-page retention map enumerating every hop document bytes could traverse exists and was written before any infrastructure (venv, lockfile, CI, storage) was selected"
    - "CI fails the build when an AGPL package is anywhere in the RESOLVED dependency set, not just top-level metadata"
    - "The AGPL gate is proven, not asserted — pdf2docx was deliberately added, the gate was observed going red, and the trap dependency was fully reverted"
  artifacts:
    - path: ".planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md"
      provides: "ENG-06 one-page data-flow retention map, git-dated before pyproject.toml in this same plan"
    - path: "tools/license_gate.py"
      provides: "AGPL scanner over installed/resolved distributions (importlib.metadata), not top-level pyproject.toml"
    - path: "tests/fixtures/agpl_gate_proof_red.txt"
      provides: "Captured proof the gate fired on pdf2docx's transitive PyMuPDF dependency"
    - path: ".github/workflows/license-gate.yml"
      provides: "CI job running the gate on every push"
  key_links:
    - from: ".github/workflows/license-gate.yml"
      to: "tools/license_gate.py"
      via: "uv sync --locked && python tools/license_gate.py"
      pattern: "license_gate\\.py"
---

<objective>
Write the one-page data-flow retention map before any infrastructure exists, then scaffold the
Python engine project and prove — not assert — that CI fails the build on any AGPL package
anywhere in the resolved dependency tree.

Purpose: ENG-06 requires the retention map to precede infrastructure selection (queue-vs-payload
and object-store-vs-tmpfs are expensive to reverse). ENG-07 requires the AGPL lockfile gate to have
actually fired once, on a known trap (`pdf2docx` → transitive `PyMuPDF`), not merely exist as
untested code. This plan is Wave 1 because every later plan needs the scaffolded project
(`pyproject.toml`, `uv.lock`) to add its own dependencies against.

Output: `RETENTION-MAP.md`, a working Python 3.13 project scaffold pinned to the versions in
`research/STACK.md`, `tools/license_gate.py`, and committed proof (red-then-green) that the gate
works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-conformance-harness-engine-spike/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the retention map before any infrastructure is selected</name>
  <files>.planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md</files>
  <read_first>
    .planning/PROJECT.md (Constraints > Privacy)
    .planning/research/ARCHITECTURE.md (§3 "Document Identity and State Under No-Retention", §"Data Flow Summary")
    .planning/research/PITFALLS.md (Pitfall 12, "Privacy leaks that are invisible until they aren't" — the nine-hop enumeration)
  </read_first>
  <action>
    Create a one-page markdown table enumerating every hop document bytes could traverse in the
    eventual web tier, following the exact enumeration ARCHITECTURE.md and PITFALLS.md Pitfall 12
    already worked out: browser -> CDN -> load balancer -> app -> queue -> worker -> subprocess ->
    scratch -> object store -> response -> CDN -> browser, plus logs and error-reporter as
    cross-cutting hops. For each hop, fill four columns: does it retain bytes, for how long, what
    is the deletion mechanism, how would you verify deletion (test, not policy). Close the
    document with two decisions this map forces, stated as explicit conclusions this plan's Task 2
    and all later plans must honor: (1) job queues carry an opaque short-TTL handle, never document
    bytes (per ARCHITECTURE.md's resolution of Conflict #3); (2) scratch space is tmpfs, not a
    persistent object store, and the server cache is content-addressed and evictable at any moment
    (per PROJECT.md's Privacy constraint). Date the document with today's date. This file must be
    written and committed before Task 2 touches `pyproject.toml`, `uv.lock`, or any CI file —
    verify by checking `git log --follow` ordering after both tasks land, or simply do Task 1's
    commit before starting Task 2.
  </action>
  <acceptance_criteria>
    File exists at the path above; contains a table with at least the following hop rows: browser,
    CDN, load balancer/app, queue, worker/subprocess, scratch, object store, logs, error reporter;
    explicitly states the queue-carries-handle-only and tmpfs-scratch-not-object-store decisions in
    prose, not just implied by the table; is dated 2026-08-12 or later.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md && grep -qi "tmpfs" .planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md && grep -qi "queue" .planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md && grep -c '|' .planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md | awk '{exit ($1 < 9 ? 1 : 0)}'</automated>
  </verify>
  <done>Retention map committed with the required hop table and both binding decisions stated, before any infrastructure file exists in the repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the Python project and prove the AGPL lockfile gate fires</name>
  <files>pyproject.toml, uv.lock, tools/license_gate.py, tests/test_license_gate.py, tests/fixtures/agpl_gate_proof_red.txt, tests/fixtures/agpl_gate_proof_green.txt, .github/workflows/license-gate.yml</files>
  <read_first>
    .planning/phases/01-conformance-harness-engine-spike/RETENTION-MAP.md (just written — infra choices here must not contradict it)
    .planning/research/STACK.md ("Installation" section and "The AGPL Trap" section, verbatim version pins)
    .planning/research/SUMMARY.md (§"Licensing policy — one rule")
  </read_first>
  <behavior>
    - `license_gate.check_installed_distributions()` returns an empty list when no installed
      distribution's `License` field or `Classifier: License ::` entries contain "AGPL" or
      "Affero" (case-insensitive substring match on both fields).
    - It returns a non-empty list of `(package_name, version)` tuples when at least one installed
      distribution's license metadata matches, e.g. after `pdf2docx` pulls in `PyMuPDF`.
    - `tools/license_gate.py` run as a script exits 0 when the list is empty, exits 1 and prints
      every offending package when the list is non-empty.
  </behavior>
  <action>
    Run `uv init --python 3.13` at the repo root if not already a uv project. Add the runtime
    dependencies pinned exactly to the versions research/STACK.md verified live against PyPI:
    `uv add pikepdf==10.11.0 playa-pdf==1.1.0 fonttools==4.63.0 uharfbuzz==0.56.0
    pypdfium2==5.12.1 pillow==12.3.0`. Add `uv add --dev pytest`. Write `tools/license_gate.py`:
    iterate `importlib.metadata.distributions()` (stdlib, no new dependency — this inspects the
    actually-installed, actually-resolved closure, which is what "anywhere in the resolved
    lockfile, not top-level metadata" requires), check each distribution's `metadata['License']`
    value and every `metadata.get_all('Classifier') or []` entry for the substrings "AGPL" or
    "Affero" case-insensitively, print any match as `package==version: <matched field>`, exit 1 if
    any match found, exit 0 otherwise. Prove it fires: `uv add pdf2docx`, `uv sync`, run
    `python tools/license_gate.py`, redirect full stdout+exit code into
    `tests/fixtures/agpl_gate_proof_red.txt` (must show `PyMuPDF` flagged for AGPL/Affero and a
    nonzero exit code recorded in the file). Then fully revert: `uv remove pdf2docx`, `uv sync`,
    rerun and capture into `tests/fixtures/agpl_gate_proof_green.txt` (exit 0, empty match list).
    Write `tests/test_license_gate.py`: one test asserting `agpl_gate_proof_red.txt` contains both
    "PyMuPDF" and ("AGPL" or "Affero"); one test asserting `agpl_gate_proof_green.txt` records exit
    code 0; one live test that imports `tools/license_gate.py` and asserts
    `check_installed_distributions()` returns `[]` against the current (reverted) environment —
    this is the regression guard going forward. Write `.github/workflows/license-gate.yml`: a job
    that runs `uv sync --locked` then `python tools/license_gate.py`, failing the build on nonzero
    exit, on every push and pull request.
  </action>
  <acceptance_criteria>
    `pyproject.toml` and `uv.lock` contain no `pdf2docx` or `PyMuPDF` entry (fully reverted);
    `python tools/license_gate.py` exits 0 against the current environment;
    `tests/fixtures/agpl_gate_proof_red.txt` exists and contains both "PyMuPDF" and
    "AGPL"/"Affero"; `tests/fixtures/agpl_gate_proof_green.txt` records a zero exit;
    `pytest tests/test_license_gate.py -q` passes; `.github/workflows/license-gate.yml` exists and
    invokes `tools/license_gate.py`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_license_gate.py -q && python tools/license_gate.py; echo "gate_exit=$?"; grep -c pdf2docx pyproject.toml | grep -qx 0</automated>
  </verify>
  <done>Gate script exists and passes against the current clean environment; the red-then-green proof is captured and committed; pyproject.toml/uv.lock are AGPL-clean; CI job wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine / CI runner ↔ PyPI | Installing third-party packages during `uv add`/`uv sync` — supply-chain trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Tampering | pyproject.toml / uv.lock resolved closure | mitigate | AGPL lockfile gate (this plan) scans every installed distribution's license metadata, not just top-level declared deps; runs in CI on every push |
| T-01-02 | Repudiation | AGPL-trap proof step (pdf2docx) | mitigate | Task 2's explicit revert-and-reverify sub-steps plus the CI gate as a permanent backstop catch it if the revert is ever incomplete |
| T-01-SC | Tampering | npm/pip installs (pikepdf, playa-pdf, fontTools, uharfbuzz, pypdfium2, pillow, pytest, pdf2docx) | mitigate | No phase-level RESEARCH.md exists for this phase (roadmap states the phase is itself the spike); research/STACK.md provides live-verified PyPI version/license data for every one of these packages (queried 2026-08-11, HIGH confidence per its own Confidence table) — treated as equivalent to the Package Legitimacy Audit gate for this specific, already-named set. `pdf2docx` is deliberately added and removed per this plan's Task 2, already analyzed by name in research/STACK.md and research/SUMMARY.md as the known AGPL trap. No blocking human-verify checkpoint added; none of these are unresearched [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
Run `pytest tests/test_license_gate.py -q` — must pass. Run `python tools/license_gate.py` —
must exit 0 against the committed environment. Confirm `git log --oneline -- RETENTION-MAP.md
pyproject.toml` shows the retention map's commit preceding pyproject.toml's first commit.
</verification>

<success_criteria>
Retention map exists and precedes all infrastructure selection. AGPL gate has fired once on a real
trap dependency and now passes clean. CI enforces the gate on every push.
</success_criteria>

<output>
Create `.planning/phases/01-conformance-harness-engine-spike/01-01-SUMMARY.md` when done
</output>
