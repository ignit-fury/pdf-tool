---
phase: 02-text-model
plan: 10
type: execute
wave: 8
depends_on: ["02-09"]
files_modified:
  - engine/identity_rewrite.py
  - tests/test_roundtrip.py
autonomous: true
requirements: [TEXT-03]

must_haves:
  truths:
    - "Gate G1 criterion 1: provenance round-trips on the corpus -- extract, locate, rewrite, re-extract returns the same run IDs and glyph records, with run IDs addressing the immutable original bytes"
    - "TEXT-03: run IDs never drift across edits -- proven via identity-rewrite, never via byte-level round-trip equality, which is explicitly rejected as a correctness test"
    - "The Phase 3 boundary is drawn explicitly in code: this module performs zero width-fitting, zero subsetting, zero substitution, zero TJ arithmetic"
  artifacts:
    - path: "engine/identity_rewrite.py"
      provides: "identity_rewrite(), null_edit_rewrite() (hex-string re-serialization), verify_roundtrip()"
  key_links:
    - from: "engine/identity_rewrite.py"
      to: "engine/index.py"
      via: "RunIndex re-indexing of the rewritten output for verify_roundtrip"
      pattern: "RunIndex\\("
    - from: "engine/identity_rewrite.py"
      to: "harness/masked_diff.py"
      via: "same-engine zero-tolerance masked_pixel_diff() reused, never a new mechanism"
      pattern: "masked_pixel_diff"
---

<objective>
Build the minimum write capability Gate G1 criterion 1 actually needs: an identity content-stream
rewrite (parse then unparse with zero semantic change) plus an optional null-edit hex-string
extension, verified by re-indexing and confirming every original run ID still resolves against the
original bytes.

Purpose: Gate G1 criterion 1 names "rewrite," which Phase 3 owns. 02-RESEARCH.md Section 9 draws the
line precisely: this plan proves the address round-trip -- parse, offset computation, ID encode,
re-parse, ID decode, re-match -- with zero width fitting, zero font subsetting, zero glyph
substitution, zero TJ arithmetic. Any of those belongs to Phase 3's rewrite engine.

Output: `engine/identity_rewrite.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-text-model/02-CONTEXT.md
@engine/index.py
@harness/masked_diff.py
@harness/run_corpus_harness.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: identity-rewrite plus the null-edit hex-string extension</name>
  <files>engine/identity_rewrite.py, tests/test_roundtrip.py</files>
  <read_first>
    02-RESEARCH.md Section 9 "Provenance round-trip -- the minimum write capability" (full section:
    the exact per-page procedure, why byte offsets legitimately change on unparse, the
    "explicitly out of scope" list, the null-edit extension recommendation)
    02-RESEARCH.md constraint list, "byte-level round-trip equality is not a valid correctness test"
    02-RESEARCH.md Component responsibilities table, "Content-stream writing (Gate G1 round-trip
    only)" row -- "Deliberately minimal in this phase"
  </read_first>
  <behavior>
    - identity_rewrite(page) calls pikepdf.parse_content_stream(page), then
      pikepdf.unparse_content_stream(ops), and writes the result back with zero semantic change.
    - null_edit_rewrite(page) does the same, except every Tj literal string operand is re-serialized
      as an equivalent hex string (identical bytes, different serialization) -- proving the address
      round-trip survives a real (if trivial) byte-level change, not just a no-op.
    - verify_roundtrip(original_index, rewritten_pdf_path) re-indexes the rewritten output via
      RunIndex and asserts every ORIGINAL run ID still resolves against the ORIGINAL bytes (never
      re-derived from the output -- TEXT-03's core rule), and that the re-indexed output produces the
      same sequence and count of runs with matching content (code, unicode, font identity,
      render_mode, x, y, advance within epsilon).
  </behavior>
  <action>
    Implement per behavior. Add a module docstring paragraph stating the Phase 3 boundary in plain
    terms: this module performs zero width-fitting, zero font subsetting, zero glyph substitution,
    zero TJ-array arithmetic -- any of those belongs to Phase 3's rewrite engine, and a task tempted
    to "just change one word to prove it" is scope-creeping into Gate G2a.
  </action>
  <acceptance_criteria>
    On a real corpus document, identity_rewrite followed by verify_roundtrip returns True;
    null_edit_rewrite produces byte-different serialization (a hex string literal is present in the
    output) with verify_roundtrip still True; a permanent negative-case test that deliberately
    changes one Tj operand's actual text during the "identity" rewrite proves verify_roundtrip
    correctly returns False.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_roundtrip.py::test_identity_rewrite_preserves_run_ids -q</automated>
  </verify>
  <done>Identity-rewrite and its verification exist, the Phase 3 boundary is durable in code, and the
  round-trip assertion is proven to actually detect a real change, not just accept everything.</done>
</task>

<task type="auto">
  <name>Task 2: Round-trip across the 17 malformed documents (Open Question 4)</name>
  <files>tests/test_roundtrip.py</files>
  <read_first>
    02-RESEARCH.md "Open Questions" item 4 (qpdf repair-on-open, part-index stability, why the run ID
    scheme deliberately used the part index rather than the stream objid)
    corpus/manifest.json malformed category (17 documents)
  </read_first>
  <action>
    Run identity_rewrite plus verify_roundtrip against all 17 malformed-category documents
    specifically -- qpdf repairs these on open, so this is the empirical test of whether the run ID
    scheme's part-index choice survives repair. Record the outcome per document. If any document's
    part indices shift, do not silently loosen the assertion to pass -- record the finding explicitly
    (a named, understood failure) matching this repo's "a check that reports green while measuring
    the wrong thing" discipline.
  </action>
  <acceptance_criteria>
    The 17-document malformed-corpus round-trip test reports a per-document pass/fail table; any
    failure is a recorded, understood finding with a reason, never a silently-loosened assertion.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_roundtrip.py -k malformed -q</automated>
  </verify>
  <done>Open Question 4 is closed with measured evidence, not left open into Phase 3.</done>
</task>

<task type="auto">
  <name>Task 3: Same-engine zero-diff verification via the existing harness</name>
  <files>tests/test_roundtrip.py</files>
  <read_first>
    harness/masked_diff.py, harness/run_corpus_harness.py (existing render_all/masked_pixel_diff
    primitives -- reused here, not reinvented, per Section 9's "Verification vehicle" instruction)
  </read_first>
  <action>
    For one representative document per major structural category (contents_array, form_xobjects,
    type0_identity_h, type3), render the original page and the identity_rewrite/null_edit_rewrite-d
    page via harness.render_diff.render_all and assert harness.masked_diff.masked_pixel_diff() == 0
    with zero tolerance -- same-engine, per the two-layer assertion; this is the ideal same-engine
    test since the same rasterizer renders the same unedited pixels twice.
  </action>
  <acceptance_criteria>
    Zero pixel diff on every sampled document for both identity_rewrite and null_edit_rewrite
    outputs, using only the existing harness primitives.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_roundtrip.py -k harness -q</automated>
  </verify>
  <done>Gate G1 criterion 1's rewrite claim is verified through the same mechanism Phase 1 built for
  exactly this purpose, not a new one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| engine.identity_rewrite <-> pikepdf.save() writing to scratch | The only write path Phase 2 has; scratch-only, never overwrites the original corpus file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-01 | Denial of Service | pikepdf.save() on a pathological (decompression-bomb-shaped) document | mitigate | Writes only to a scratch path already covered by 02-09's per-document wall-clock cap; no new surface introduced here |
| T-02-06 | Tampering (scope creep) | This module is the one most able to accidentally grow into Phase 3's rewrite engine | mitigate | The explicit docstring boundary plus Task 1's negative-case test are the enforcement mechanism -- there is no automated way to detect "this PR quietly started doing width-fitting" other than the recorded boundary and code review discipline |
</threat_model>

<verification>
`pytest tests/test_roundtrip.py -q` passes, including the malformed-corpus and harness-verification
subsets.
</verification>

<success_criteria>
Gate G1 criterion 1 is satisfied: provenance round-trips on the corpus via identity-rewrite, never via
byte-level equality, with the Phase 3 boundary drawn explicitly in code. This closes Phase 2's last
requirement.
</success_criteria>

<output>
Create `.planning/phases/02-text-model/02-10-SUMMARY.md` when done
</output>
