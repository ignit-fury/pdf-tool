---
phase: 02-text-model
plan: 08
type: execute
wave: 6
depends_on: ["02-07", "02-02"]
files_modified:
  - engine/classify_page.py
  - engine/classify_run.py
  - tests/test_classify.py
autonomous: true
requirements: [CLAS-01, CLAS-02, CLAS-03, CLAS-04, CLAS-05]

must_haves:
  truths:
    - "CLAS-01: every page classifies into one of four uneditable buckets or editable, using three signals, with image coverage never exceeding 1.0"
    - "CLAS-02: an OCR'd scan classifies as searchable-not-editable, never as editable text, and the Tr=7 invisibility rule is proven on a synthetic fixture since the corpus itself has no Tr=7 glyph"
    - "CLAS-03: vector-outlined text has its own bucket, distinct from a scan, backed by the genuine fixture 02-02 sourced"
    - "CLAS-04: every run carries one of three states before any edit, with symbolic/Type3/no-ToUnicode/shared-XObject/RTL runs named specifically"
    - "CLAS-05: the operation is refused, never the document -- proven on the constructed mixed fixture"
  artifacts:
    - path: "engine/classify_page.py"
      provides: "image_coverage() (union, never sum), classify_page() -> one of six PageBucket values"
    - path: "engine/classify_run.py"
      provides: "classify_run() -> RunVerdict, classify_document() -> per-page/per-run refusal, never whole-document"
  key_links:
    - from: "engine/classify_run.py"
      to: "engine/shared_xobjects.py"
      via: "shared_form_xobjects() cross-referenced by stream_id, the only consumer of this utility"
      pattern: "shared_form_xobjects"
    - from: "engine/classify_page.py"
      to: "engine/walker.py"
      via: "GlyphRecord.render_mode / .visible fields"
      pattern: "render_mode"
---

<objective>
Build the four-bucket page classifier (CLAS-01..03) and the three-state per-run classifier
(CLAS-04..05), the last stage before Phase 2's data model is complete.

Purpose: nearly free once the run index exists, per the ROADMAP's stated rationale for shipping
classification with the text model rather than as its own phase -- but it inherits every fixture gap
this phase spent two plans fixing. This is where 02-02's constructed fixtures and 02-05's
shared_xobjects utility finally get consumed.

Output: `engine/classify_page.py`, `engine/classify_run.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-text-model/02-CONTEXT.md
@engine/walker.py
@engine/encoding_table.py
@engine/shared_xobjects.py
@tests/fixtures/mixed_scanned.pdf
@tests/fixtures/render_mode_7.pdf
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine/classify_page.py -- CLAS-01 four-bucket classification, image-coverage union fix</name>
  <files>engine/classify_page.py, tests/test_classify.py</files>
  <read_first>
    02-RESEARCH.md Section 7 up through "Proposed thresholds" (the three signals defined
    operationally, the bucket rule table)
    02-RESEARCH.md Pitfall 5 "Summing image bounding boxes for coverage" (measured to exceed 1.0 --
    2.749 on nasa_graphics_standards_manual.pdf -- before the union fix)
    02-RESEARCH.md Security Domain table, V5 row (the OCG /OCProperties /D /OFF check, low frequency)
  </read_first>
  <behavior>
    - image_coverage(page) -> float, bounded [0, 1], via an 80x80 boolean occupancy grid over the
      CropBox-clipped union of image-XObject bboxes -- never a sum of bbox areas.
    - classify_page(glyph_records, page) -> one of {scan_no_text, ocr_scan, vector_outlined, empty,
      editable, mixed_degraded}, per Section 7's threshold table, using visible-glyph count
      (render_mode not in {3, 7}, approximated against CropBox intersection since
      GraphicState.clipping_path is not populated in playa 1.1.0 -- log this as a known limitation,
      not a silent gap), invisible-glyph count, and image_coverage, plus the OCG-off check via
      mcstack cross-referenced against /Root /OCProperties /D /OFF.
  </behavior>
  <action>
    Implement per behavior, transcribing the Pitfall-5 fix exactly. Use P is approximately 200
    (path-object count) as the vector-outlined discriminator (Assumption A3), now validatable
    against 02-02's sourced/constructed vector_outlined_text_sample.pdf fixture.
  </action>
  <acceptance_criteria>
    nasa_graphics_standards_manual.pdf classifies as ocr_scan, not vector_outlined; a
    sum-instead-of-union mutation reproduces the measured 2.749 coverage value on that document, kept
    as a permanent negative-case test; invoice_book_1842.pdf's 52 pages classify as 7 scan_no_text
    plus 45 ocr_scan, matching the measured counts exactly.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_classify.py::test_buckets_and_coverage_bounded tests/test_classify.py::test_ocr_scan_fixture -q</automated>
  </verify>
  <done>Page classification is correct on the two documents 02-RESEARCH.md used to validate it, and
  the coverage-formulation bug is proven fixed, not merely described as fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLAS-03 vector-outlined bucket and CLAS-02's Tr=7 synthetic-fixture proof</name>
  <files>engine/classify_page.py, tests/test_classify.py</files>
  <read_first>
    corpus/manifest.json's vector_outlined_text_sample.pdf entry (02-02)
    tests/fixtures/render_mode_7.pdf (02-02)
    02-RESEARCH.md "Every check..." table, CLAS-02 and CLAS-03 rows (the explicit "no mutation can
    make this check meaningful until a fixture exists" and "must not claim to detect a Tr=7 bug"
    warnings this task closes)
  </read_first>
  <action>
    AMENDED 2026-08-16 (controller, before this task's own dispatch). The vector-outlined half is
    ALREADY DONE, built pre-dispatch while resolving Task 1's own finding that
    vector_outlined_text_sample.pdf did not cross P_PATH_OBJECT_THRESHOLD (see Task 1's ledger
    entries and commits 88bece7, aff2027) -- Task 2 would otherwise have hit the exact same wall.
    `tests/test_classify.py::test_vector_outlined_fixture_path_count_crosses_threshold` already
    asserts the corpus document classifies vector_outlined (distinct from ocr_scan/scan_no_text by
    construction, since Bucket is a Literal and only one value is asserted), reviewed clean across
    two rounds. Do not re-write it or rename it to the literal `test_vector_outlined_fixture` --
    the descriptive name matches this project's own convention for finding/negative-case tests
    elsewhere in this file and this phase.

    Also corrected: tests/fixtures/render_mode_7.pdf does NOT have "a single glyph" -- it has 26,
    all render_mode 7, all invisible. Write the Tr=7 visibility test against the real count.

    Write a Tr=7 visibility test against tests/fixtures/render_mode_7.pdf asserting all 26 of its
    glyphs count as invisible (visible == False), and demonstrate the render-mode mutation
    (render_mode != 3 instead of render_mode not in (3, 7)) now goes RED on this synthetic fixture --
    explicitly closing the stated Validation Architecture gap. Use pytest's `monkeypatch` fixture to
    genuinely patch `engine.walker.INVISIBLE_RENDER_MODES` to `frozenset({3})` and re-run
    `glyph_records` on the same fixture, rather than reimplementing the old wrong predicate inline --
    the mutation must exercise the real code path, not a hand-copied stand-in that could drift from
    it.
  </action>
  <acceptance_criteria>
    `pytest tests/test_classify.py::test_vector_outlined_fixture_path_count_crosses_threshold -q`
    passes (already true, confirm it still is); the new Tr=7 test and its mutation demonstration
    both pass, and the mutation demonstrates a failure that the corpus alone could never produce
    (zero real Tr=7 glyphs exist anywhere in the 217-document corpus).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_classify.py -k "vector_outlined or tr7 or tr_7 or render_mode_7" -q</automated>
  </verify>
  <done>CLAS-03 and CLAS-02's stated fixture gaps are both closed with real, demonstrated-failing
  checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: engine/classify_run.py -- CLAS-04 three-state run verdict, CLAS-05 refuse-operation-not-document</name>
  <files>engine/classify_run.py, tests/test_classify.py</files>
  <read_first>
    02-RESEARCH.md Architecture Patterns diagram (CLAS-04 derives from the fired encoding branch --
    it is "free" once Section 1 exists)
    02-CONTEXT.md CLAS-04/CLAS-05 phrasing and ROADMAP.md Phase 2 success criterion 3 (the 37-of-40
    shape)
    engine/shared_xobjects.py (02-05, for the "shared" not-editable reason)
    engine/encoding_table.py's FontVerdict/glyph_verdict
    tests/fixtures/mixed_scanned.pdf (02-02)
  </read_first>
  <behavior>
    - classify_run(run_record, font_verdict, shared_xobject_ids) -> RunVerdict in
      {editable_original, editable_substitution, not_editable} with a named reason string when
      not_editable -- symbolic, Type3, no-ToUnicode, shared-Form-XObject and RTL each get their own
      distinct reason string, never a generic "not editable".
    - classify_document(pdf) -> DocumentClassification refuses per-page/per-run, never the whole
      document -- CLAS-05's shape.
  </behavior>
  <action>
    Implement classify_run and classify_document, calling engine.shared_xobjects.
    shared_form_xobjects once per document and cross-referencing its result by GlyphRecord.stream_id
    against each run -- this is where 02-05's utility gets consumed for the first time, staying
    engine-internal (never wired into tools/probe_corpus.py, per the Architectural Responsibility
    Map's explicit prohibition).
  </action>
  <acceptance_criteria>
    On tests/fixtures/mixed_scanned.pdf, exactly the constructed editable pages report
    editable_original/editable_substitution runs and exactly the 3 scanned pages report all-
    not_editable runs, while classify_document never returns a whole-document refusal for any page
    count; a permanent negative-case test proves the CLAS-05 mutation (collapse to
    any(page is scanned) -> document uneditable) is wrong; a run sourced from
    irs_1040_instructions.pdf's shared XObject verdicts not_editable with a reason naming the
    43-page share count.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_classify.py -q</automated>
  </verify>
  <done>CLAS-04 and CLAS-05 are both implemented and proven on real and constructed fixtures, closing
  out Phase 2's requirement list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| engine.classify_page / engine.classify_run <-> the walker's full output for a document | Consumes already-parsed provenance; first stage whose output (verdicts, reasons) is the shape a future UI (Phase 4, CLAS-06/07) will eventually show a user directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-04 | Information Disclosure | RunVerdict/PageBucket reason strings | mitigate | Reason is a closed enum of category strings (e.g. "shared across 43 pages", "no ToUnicode mapping"), never a quoted glyph or run's decoded text -- this is the control that prevents a future Phase 4 leak, established here since these are the first values a user-facing surface will eventually render |
| T-02-01 | Denial of Service | image_coverage's 80x80 occupancy grid on a page with an extreme image-XObject count | accept | O(images) per Section 7's own complexity note; no corpus document approaches a pathological count, and the per-document glyph-count/wall-clock cap (02-09) is the general backstop |
</threat_model>

<verification>
`pytest tests/test_classify.py -q` passes, including every corpus-derived and constructed-fixture
case and all named negative tests (Pitfall 5, CLAS-02 Tr=7, CLAS-05 collapse).
</verification>

<success_criteria>
CLAS-01 through CLAS-05 are all implemented and proven, closing the last of Phase 2's 13 requirement
IDs. Every run in the corpus carries a three-state verdict before any edit is attempted.
</success_criteria>

<output>
Create `.planning/phases/02-text-model/02-08-SUMMARY.md` when done
</output>
