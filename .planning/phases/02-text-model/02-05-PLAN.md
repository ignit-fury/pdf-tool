---
phase: 02-text-model
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified:
  - engine/walker.py
  - engine/shared_xobjects.py
  - tests/test_outside_contents.py
autonomous: true
requirements: [TEXT-06]

must_haves:
  truths:
    - "TEXT-06: text located outside /Contents -- Form XObjects, annotation appearance streams, tiling patterns, Type3 CharProcs -- is found by the walker"
    - "Self-referencing and deeply-nested Form XObjects are bounded by a visited set and an explicit depth cap rather than crashing"
    - "Shared Form XObjects are detected as a document-level utility (never resource-name-keyed) for the classifier to consume in 02-08 -- the walker itself does not decide editability"
  artifacts:
    - path: "engine/shared_xobjects.py"
      provides: "shared_form_xobjects(pdf) -> {objid: page_count}, keyed by object identity, never resource name"
    - path: "engine/walker.py"
      provides: "recursion into Do (Form XObject), /Annots /AP /N, tiling patterns, Type3 /CharProcs, each owning its own LazyInterpreter"
  key_links:
    - from: "engine/walker.py"
      to: "engine/playa_boundary.py"
      via: "a fresh LazyInterpreter constructed at every recursion level, never Page.flatten()"
      pattern: "from engine\\.playa_boundary import"
    - from: "engine/shared_xobjects.py"
      to: "pikepdf.Object.objgen"
      via: "object identity, never the resource dictionary key name"
      pattern: "objgen\\[0\\]"
---

<objective>
Extend the walker to recurse into the three text locations that sit outside a page's own /Contents --
Form XObjects, annotation appearance streams, tiling patterns, Type3 CharProcs -- with a depth cap and
a per-branch visited set, and build the shared-Form-XObject detector the classifier will consume.

Purpose: 02-RESEARCH.md Section 5 measured that Page.flatten() discards _curpos/streamid for
everything below the top level (Pitfall 4), so the walker must own this recursion itself. Shared
Form XObjects are common and are running headers/footers, the most visible text on a page
(irs_1040_instructions.pdf: one XObject referenced by 43 of 126 pages).

Output: engine/walker.py extended with recursion, engine/shared_xobjects.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-text-model/02-CONTEXT.md
@engine/walker.py
@engine/playa_boundary.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine/shared_xobjects.py -- shared Form XObject detection</name>
  <files>engine/shared_xobjects.py, tests/test_outside_contents.py</files>
  <read_first>
    02-RESEARCH.md Section 5 "Shared Form XObjects -- detection and measured cost" (full section plus
    the verbatim code example) and its measured table (irs_1040_instructions.pdf: 126 pages, 8 shared,
    max 43 page-references; irs_publication_17.pdf: max 47)
    02-RESEARCH.md "Every check..." table, TEXT-06 shared-XObject mutation row (the Phase-1-echoing
    "keyed by a string that split one product into two buckets" warning)
  </read_first>
  <behavior>
    - shared_form_xobjects(pdf) returns a dict mapping objid to the number of distinct pages
      referencing it, for every objid referenced by more than one page, keyed by
      xobject.objgen[0] (object identity), never by the resource dictionary's local name (Fm0 on
      page 3 and Fm0 on page 9 are frequently different objects; the same object frequently carries
      different names on different pages).
  </behavior>
  <action>
    Transcribe 02-RESEARCH.md Section 5's code example into engine/shared_xobjects.py. Write
    tests/test_outside_contents.py::test_shared_form_xobject_detection_matches_measured_counts
    asserting corpus/public/irs_1040_instructions.pdf and corpus/public/irs_publication_17.pdf report
    max page-reference counts of exactly 43 and 47. Add a permanent negative-case test proving a
    resource-name-keyed mutation is wrong on irs_1040_instructions.pdf (the same object under
    different resource names on different pages causes a resource-name-keyed detector to
    undercount).
  </action>
  <acceptance_criteria>
    Measured counts reproduce exactly (43, 47); the resource-name-keyed mutation test demonstrates a
    lower, wrong count than the objgen-keyed detector on the same document.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_outside_contents.py::test_shared_form_xobject_detection_matches_measured_counts -q</automated>
  </verify>
  <done>Shared Form XObjects are detected by object identity, matching the measured corpus counts
  exactly, ready for 02-08's classifier to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: walker recursion -- Form XObjects, annotation AP, tiling patterns, Type3 CharProcs</name>
  <files>engine/walker.py, tests/test_outside_contents.py</files>
  <read_first>
    02-RESEARCH.md Section 5 "The four locations, and how each is reached" (table: how each is
    reached, playa's support level, and the ID path segment each contributes)
    02-RESEARCH.md Section 5 "Consequence for the interpreter" (own the recursion; add a depth cap;
    reuse flatten()'s per-branch visited-set semantics but construct them ourselves)
    02-RESEARCH.md Pitfall 4 "Losing provenance below the top level"
    02-RESEARCH.md Security Domain table, "Self-referencing Form XObject" row (CVE-2026-48155 shape)
    engine/run_id.py (the xobj/annot/pattern/charproc path segments encode_run_id already supports)
  </read_first>
  <behavior>
    - walk_document(pdf, doc) walks every page and, in addition to the page's own /Contents, recurses
      into: Form XObjects invoked via Do (CTM composed as /Matrix times CTM, clipped to /BBox; path
      segment x{objid}); annotation /AP /N (resolving a dict-by-/AS when /AP/N is itself a dict; path
      segment a{annotIdx}:{apState}); tiling patterns invoked via scn/SCN with a pattern colour space
      (path segment t{patternObjid}); Type3 /CharProcs content streams (path segment
      y{charProcName}), reusing playa's existing Type3Interpreter.
    - Each recursion level constructs its own fresh LazyInterpreter (never Page.flatten(), per
      Pitfall 4) so stream_id and byte offsets stay correct for every level.
    - Recursion uses a visited set constructed per branch (parents union {stream_id}) plus an
      explicit depth cap, a named constant such as MAX_RECURSION_DEPTH = 64; exceeding either aborts
      that one branch with a logged reason, never raising past the caller.
  </behavior>
  <action>
    AMENDED 2026-08-14 (controller): the API names below were written before Wave 3's coalescing
    ruling. What actually shipped, and what you extend:
      - `engine.playa_boundary.walk_page(page, doc)` is a GENERATOR yielding
        `(part_offset, part_index, operands, text_obj)` per text-showing operator over the page's
        COALESCED /Contents, with the two-pass alignment tripwire asserted on every yield. It is the
        boundary, not the walker, and this task does not redefine it.
      - `engine.walker.glyph_records(page, doc) -> list[GlyphRecord]` is the walker's entry point.
        THIS is what you extend with recursion.
      - `walk_part` does NOT exist — it was deleted with the per-part design the coalescing ruling
        rejected. Do not import it.
    Implement the recursion in engine/walker.py, extending glyph_records and introducing
    walk_document as needed. Use corpus/public/irs_1040_instructions.pdf's known glyph-at-a-time Form
    XObject I1 (emitting "Futu" then "r" at byte offsets 673 and 679) as a concrete fixture. Construct
    a minimal 2-page synthetic pikepdf fixture, built inline in the test rather than committed as a
    file, with a Form XObject that Do-invokes itself, to exercise the cycle guard.
  </action>
  <acceptance_criteria>
    walk_document on irs_1040_instructions.pdf finds glyph records inside XObject I1 at byte offsets
    673 and 679; the self-referencing synthetic fixture walks to completion without a Python
    RecursionError, aborting only the cyclic branch with a logged reason; walk_document completes
    without exception across all 19 form_xobjects and all 13 annotation_appearance_streams corpus
    documents.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_outside_contents.py -q</automated>
  </verify>
  <done>The walker finds text in every location outside a page's own /Contents, bounded against
  cyclic and deeply-nested XObject graphs, without ever deciding editability itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| engine.walker recursion <-> Form XObject / annotation / pattern / Type3 graphs in untrusted-shaped PDFs | The graph-traversal surface most exposed to adversarial structure (cycles, depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-02 | Denial of Service | Self-referencing Form XObject causing unbounded recursion (CVE-2026-48155 shape) | mitigate | Per-branch visited set constructed by the walker itself (playa's flatten() has a visited set but no depth cap, and we do not use flatten() per Pitfall 4) |
| T-02-03 | Denial of Service | A deeply nested (500+) Form XObject chain blowing Python's recursion limit | mitigate | Explicit named depth-cap constant; exceeding it aborts that branch with a logged reason rather than raising |
| T-02-04 | Information Disclosure | Decoded glyph/run text in a logged recursion-abort reason | mitigate | Abort reasons name the branch (xobject id, depth, cycle) never glyph or run text, consistent with 02-04's grep-based logging discipline |
</threat_model>

<verification>
`pytest tests/test_outside_contents.py -q` passes, including the objgen-vs-resource-name negative
case and the self-referencing-XObject cycle guard.
</verification>

<success_criteria>
Text in Form XObjects, annotation appearance streams, tiling patterns and Type3 CharProcs is found;
cyclic and deep XObject graphs are bounded rather than crashing; shared-XObject detection is available
as a standalone document-level utility for 02-08's classifier.
</success_criteria>

<output>
Create `.planning/phases/02-text-model/02-05-SUMMARY.md` when done
</output>
