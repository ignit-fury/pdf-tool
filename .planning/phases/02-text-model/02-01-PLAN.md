---
phase: 02-text-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/conftest.py
  - tests/test_conftest_smoke.py
  - engine/__init__.py
  - .github/workflows/tests.yml
autonomous: true
requirements: [TEXT-05]

must_haves:
  truths:
    - "pytest has a config (testpaths, a `corpus` marker) and a shared conftest.py so corpus-wide integration tests can be selected/excluded deliberately"
    - "mypy 2.3.0 is installed, configured strict on the engine package, and runs in CI — the necessary condition for TEXT-05, which 02-03 enforces concretely"
    - "the engine/ package exists so every downstream plan has somewhere to put code, and mypy has something to check"
  artifacts:
    - path: "tests/conftest.py"
      provides: "manifest(), corpus_dir(), docs_with_category(), open_doc() shared fixtures"
    - path: "engine/__init__.py"
      provides: "the engine package marker; empty by design, populated by every plan from 02-03 onward"
    - path: ".github/workflows/tests.yml"
      provides: "pytest step (already present) plus a new mypy engine/ step"
  key_links:
    - from: ".github/workflows/tests.yml"
      to: "engine/"
      via: "uv run --frozen mypy engine/ step"
      pattern: "mypy engine"
---

<objective>
Close the two Wave 0 gaps that block every other Phase 2 plan: no pytest config/shared fixtures, and
no type checker installed (TEXT-05 is unverifiable without one). Create the `engine/` package that
every subsequent plan writes into.

Purpose: this is pure tooling debt inherited from Phase 2 research — 37 existing tests already run in
CI (fixed by the researcher, see `.github/workflows/tests.yml` git history), but there is still no
`conftest.py`, no `[tool.pytest.ini_options]`, and no `mypy`. Nothing downstream can be verified
without this landing first.

Output: `tests/conftest.py`, `pyproject.toml` pytest+mypy config, `mypy==2.3.0` installed, a `mypy`
CI step, `engine/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-text-model/02-CONTEXT.md
@.planning/phases/02-text-model/02-VALIDATION.md
@corpus/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest config + shared conftest.py</name>
  <files>pyproject.toml, tests/conftest.py, tests/test_conftest_smoke.py</files>
  <read_first>
    .planning/phases/02-text-model/02-VALIDATION.md (Test Infrastructure table, Wave 0 gaps table)
    .planning/phases/02-text-model/02-RESEARCH.md §Validation Architecture "Wave 0 gaps" bullet list
    pyproject.toml (current — no [tool.pytest.ini_options] section exists)
    corpus/manifest.json (schema: array of {filename, sha256, tier, categories, source_url, license, weight_class, notes})
  </read_first>
  <action>
    Add `[tool.pytest.ini_options]` to pyproject.toml: `testpaths = ["tests"]`, `markers = ["corpus:
    exercises the full corpus/public tier (slower, excluded from the quick per-commit run)"]`. Create
    `tests/conftest.py` with pytest fixtures: `manifest()` — session-scoped, loads
    `corpus/manifest.json` via `json.loads`; `corpus_dir()` — returns `Path("corpus/public")`;
    `docs_with_category(category: str) -> list[dict]` — a plain helper function (not a fixture,
    importable directly) filtering `manifest` entries whose `categories` list contains `category`;
    `open_doc(filename: str)` — a helper returning `pikepdf.open(corpus_dir() / filename)`, caller
    responsible for closing (mirrors the existing per-file `pikepdf.open`/`.close()` pattern already
    used in `tools/probe_corpus.py`). Write `tests/test_conftest_smoke.py`: asserts `len(manifest())
    == 216`, `corpus_dir().is_dir()`, and `docs_with_category("type3")` returns at least 12 entries
    (the measured corpus count).
  </action>
  <acceptance_criteria>
    `pytest --collect-only -q` shows no `PytestUnknownMarkWarning` for the `corpus` marker;
    `pytest tests/test_conftest_smoke.py -q` passes; the smoke test's category-count assertion matches
    the measured corpus distribution (`type3` == 12) from `02-RESEARCH.md`'s corpus category table.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen pytest tests/test_conftest_smoke.py -q</automated>
  </verify>
  <done>pytest is configured with testpaths and a registered `corpus` marker; conftest.py exposes
  manifest/corpus_dir/docs_with_category/open_doc for every later test file to reuse without
  re-deriving corpus paths.</done>
</task>

<task type="auto">
  <name>Task 2: Install mypy, scope it strict to engine/, wire CI</name>
  <files>pyproject.toml, uv.lock, engine/__init__.py, .github/workflows/tests.yml</files>
  <read_first>
    .planning/phases/02-text-model/02-RESEARCH.md §"Package Legitimacy Audit" (mypy: Approved, clean
    slopcheck) and §"Code Examples > Pattern 3" (the mypy strict setting this enforces)
    .planning/phases/02-text-model/02-VALIDATION.md (Test Infrastructure row: "Type checker mypy not
    installed. TEXT-05 is unverifiable without it.")
    .github/workflows/tests.yml (current — pytest step only)
    tools/license_gate.py (what re-running it after a lockfile change checks)
  </read_first>
  <action>
    Run `uv add --dev mypy==2.3.0`. Create `engine/__init__.py` as an empty package marker — every
    plan from 02-03 onward adds real modules here; this task's only job is to give mypy something to
    check and downstream plans a package to write into. Add `[tool.mypy]` to pyproject.toml:
    `strict = true`, `files = ["engine"]` (scoped to the engine package only — never `tools/`,
    `tests/`, `spike/`, `harness/`, per the constraint that this is an engine-internal contract, not a
    repo-wide one). Add a step to `.github/workflows/tests.yml` after the pytest step: `- name: Type
    check engine package` / `run: uv run --frozen mypy engine/`. Re-run `python tools/license_gate.py`
    locally against the updated `uv.lock` to confirm mypy's transitive deps (`pathspec`,
    `mypy_extensions`, etc.) introduce no AGPL — this package is already vetted `Approved` in
    02-RESEARCH.md's Package Legitimacy Audit table, so no blocking checkpoint is needed here, only
    confirmation the lockfile stayed clean.
  </action>
  <acceptance_criteria>
    `uv run --frozen mypy engine/` exits 0 against the empty package; `python tools/license_gate.py`
    exits 0 against the regenerated `uv.lock`; `pyproject.toml` pins `mypy==2.3.0` exactly (not a
    range); `.github/workflows/tests.yml` contains a step running `mypy engine/`.
  </acceptance_criteria>
  <verify>
    <automated>uv run --frozen mypy engine/ && python tools/license_gate.py</automated>
  </verify>
  <done>mypy strict is installed, scoped to engine/, green in CI, and license-gate-clean — TEXT-05 has
  the tooling it needs; 02-03 implements and enforces the actual type contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dev/CI tooling ↔ PyPI | A new dev-only dependency (`mypy` and its transitive deps) is added to the resolved lockfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-SC | Tampering (supply chain) | `mypy` PyPI install and its transitive deps | mitigate | Already vetted `Approved` by `slopcheck` in 02-RESEARCH.md's Package Legitimacy Audit (13+ yr old package, ~30M/wk downloads, github.com/python/mypy); this task re-runs `tools/license_gate.py` against the regenerated lockfile as the concrete gate, not a manual judgment call |
| T-02-01 | Information Disclosure | CI workflow file changes | accept | No secrets touched; `.github/workflows/tests.yml` already runs on every push per its existing trigger |
</threat_model>

<verification>
`uv run --frozen pytest tests/test_conftest_smoke.py -q` passes. `uv run --frozen mypy engine/`
exits 0. `python tools/license_gate.py` exits 0. `.github/workflows/tests.yml` runs both pytest and
mypy on every push.
</verification>

<success_criteria>
Every downstream Phase 2 plan has a shared conftest.py to build tests against, a `corpus` marker to
separate fast unit tests from slow corpus-wide integration tests, an `engine/` package to write into,
and a working `mypy --strict` gate in CI — closing the two tooling gaps that would otherwise make
TEXT-05 and every "demonstrate this check failing" requirement unverifiable.
</success_criteria>

<output>
Create `.planning/phases/02-text-model/02-01-SUMMARY.md` when done
</output>
