---
phase: 02-text-model
plan: 06
type: execute
wave: 3
depends_on: ["02-03", "02-02"]
files_modified:
  - engine/encoding_table.py
  - tests/test_encoding_table.py
autonomous: true
requirements: [TEXT-04]

must_haves:
  truths:
    - "D-04: ambiguous encoding refuses and logs which branch was ambiguous -- every font resolution names its branch_id"
    - "D-04: the measured refusal rate is recorded and bounded two-sided, so the check cannot pass by refusing nothing at all"
    - "TEXT-04: Type1/CFF symbolic+/Encoding resolves (does not refuse); TrueType symbolic+/Encoding refuses -- collapsing the two is proven to jump the refusal rate from about 2% to about 23%+"
    - "D-05 groundwork: glyph_verdict marks a single bad glyph refused without refusing its whole font or run"
  artifacts:
    - path: "engine/encoding_table.py"
      provides: "resolve_font() (the full B1-B6/T1/TT/C-1..C6/T3-a dispatch), glyph_presence() (A-6), glyph_verdict() (A-8, D-05)"
  key_links:
    - from: "engine/encoding_table.py"
      to: "engine/records.py"
      via: "FontVerdict/GlyphVerdict shapes consumed by glyph fields"
      pattern: "def resolve_font"
    - from: "(future) engine/clusterer.py"
      to: "engine/encoding_table.py"
      via: "glyph_verdict() as the D-05 split signal"
      pattern: "glyph_verdict"
---

<objective>
Build the forward encoding decision table: a pikepdf-only, playa-free structural dispatch from a font
dictionary to a branch ID and an editability verdict, per 02-RESEARCH.md Section 1's full table.

Purpose: TEXT-04 requires the fired branch logged per font, never a guess. This module runs BEFORE
the walk, per font -- it is a parallel structural determination from the font dictionary, not a
re-implementation of playa's internal decode (per the Architectural Responsibility Map: "must NOT
call into the walker"). It also carries D-04's most important corrected finding: collapsing Type1 and
TrueType's symbolic+/Encoding handling wrongly refuses 23% of the corpus instead of the correct ~2%.

Output: `engine/encoding_table.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-text-model/02-CONTEXT.md
@engine/types.py
@engine/records.py
@tools/probe_corpus.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Simple-font branches (T1-a..d, TT-a..f) plus the refusal list A-1, A-2, A-3, A-5, A-7</name>
  <files>engine/encoding_table.py, tests/test_encoding_table.py</files>
  <read_first>
    02-RESEARCH.md Section 1 full simple-font table: the B1-B6 base-encoding preliminary, the
    /Differences overlay warning (a pairwise integer/name parse is wrong), and the T1-a..T1-d,
    TT-a..TT-e resolution table with corpus occurrence counts
    02-RESEARCH.md Section 2 "The full refusal list" table, A-1 through A-8
    02-RESEARCH.md Pitfall 2 "Refusing all Symbolic + /Encoding fonts" (the exact failure mode and
    the 4-doc vs 50-doc numbers)
    tools/probe_corpus.py, _flags_are_symbolic() (the symbolic-bit-check pattern to mirror; do not
    import from tools/, this module is standalone per the Architectural Responsibility Map)
  </read_first>
  <behavior>
    - resolve_font(font_dict) -> FontVerdict(branch_id: str, editable: bool, substitution: bool,
      reason: str | None) dispatches top-to-bottom per Section 1's table, first match wins.
    - Type1/CFF symbolic with /Encoding present (T1-b) resolves editable=True -- the corrected
      finding; /Differences overlays the font's built-in encoding unconditionally per section
      9.6.6.2, Symbolic only selects which base the differences overlay.
    - TrueType symbolic with /Encoding present (TT-c) refuses, reason names branch "TT-c" (A-1).
    - TrueType symbolic with only a (3,1) cmap and no (3,0)/(1,0) refuses as "TT-d" (A-2); no cmap at
      all refuses as "TT-e" (A-3).
    - A symbolic simple font with no embedded program refuses as "NOEMB" (A-5).
    - A span covered by /ActualText is marked not-editable, reason "ACTUALTEXT" (A-7), and is never
      used as an edit target.
  </behavior>
  <action>
    Implement the B1-B6 base-encoding resolution plus /Differences overlay (a mixed array of integers
    and names, where each integer resets the running code counter -- implement this correctly, not as
    a naive pairwise zip), then the T1-a..T1-d and TT-a..TT-f name/glyph resolution branches, using
    only pikepdf object access (Subtype, /FontDescriptor /Flags symbolic bit, /Encoding presence and
    shape, /FontFile/FontFile2/FontFile3 embedding presence). No playa import anywhere in this file.
  </action>
  <acceptance_criteria>
    `pytest tests/test_encoding_table.py -k "not corpus" -q` passes, with at least one assertion per
    named branch (T1-a, T1-b, T1-c, T1-d, TT-a, TT-b, TT-c, TT-d, TT-e) against either a synthetic
    pikepdf font-dict fixture or a named real corpus document; a permanent negative-case test proves
    the Pitfall-2 mutation (branch on "symbolic and has_encoding" before checking /Subtype) pushes the
    refusal population from the correct ~4-document TT-c set toward the wrong ~54-document set.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_encoding_table.py -k "not corpus" -q</automated>
  </verify>
  <done>Simple-font branches resolve correctly, with Type1 and TrueType's ambiguity handling proven
  distinct via a permanent regression test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type0/CID branches (C-1..C-6), Type3 (T3-a), glyph-presence downgrade (A-6), per-glyph verdict (A-8, D-05)</name>
  <files>engine/encoding_table.py, tests/test_encoding_table.py</files>
  <read_first>
    02-RESEARCH.md Section 1 "Type0 / CID fonts" table, C-1 through C-6
    02-RESEARCH.md Section 1 T3-a Type3 row (glyph name via /Differences indexes /CharProcs; width
    from /Widths in glyph space scaled by /FontMatrix, never divided by 1000)
    02-RESEARCH.md Section 2, A-6 ("resolvable -- the glyph-presence check catches it. Downgrade to
    editable-with-substitution, do not refuse") and A-8 (per-glyph refusal, D-05)
    02-RESEARCH.md "Don't Hand-Roll" table, fontTools and /CIDToGIDMap rows
  </read_first>
  <behavior>
    - resolve_font extended: Type0 composite fonts route through C-1 (codespace-range byte-width
      detection off the CMap, never hardcoded to 2 bytes), C-2 (Identity-H/V vs a named CMap table
      lookup), C-3a (/CIDToGIDMap Identity vs a binary big-endian-2-byte-per-CID stream -- handle
      both, not just Identity), C-3b (CFF charset for CIDFontType0C), C-4 (/W nested run-length with
      /DW default 1000, never /Widths/MissingWidth), C-6 (mixed-byte-width codespace -> refuse).
    - Type3 (T3-a) resolves via /Encoding /Differences -> /CharProcs name lookup.
    - glyph_presence(font_verdict, code, font_program_bytes) uses fontTools (ttLib for TrueType/CFF,
      t1Lib for Type1) to check whether the resolved glyph actually exists in the embedded program;
      when it does not, downgrade (never refuse) to editable=True, substitution=True.
    - glyph_verdict(font_verdict, glyph_record) -> GlyphVerdict folds in A-8: a glyph with no
      /ToUnicode mapping and a non-AGL glyph name (e.g. "g47") refuses that single glyph only --
      D-05's per-glyph split signal, consumed later by the clusterer, never the whole font or run.
  </action>
  <action>
    Implement C-1..C-6, T3-a, glyph_presence, glyph_verdict. Use corpus/public/irs_publication_17.pdf
    (CID-keyed CFF, live CJK per 02-RESEARCH.md's PLAYA-DECISION.md-cited verification) and
    corpus/public/govdocs1_000_000135.pdf (Type3 glyphs with cid=0, text='') as test fixtures.
  </action>
  <acceptance_criteria>
    irs_publication_17.pdf's CID-keyed CFF font resolves via C-3b (CFF charset, not
    /CIDToGIDMap); a synthetic font-dict fixture with mixed 1-byte/2-byte codespace ranges refuses
    per C-6; govdocs1_000_000135.pdf's Type3 glyphs trigger glyph_verdict's A-8 per-glyph refusal
    without refusing the whole font.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_encoding_table.py -k "not corpus" -q</automated>
  </verify>
  <done>Type0/CID and Type3 branches resolve correctly; per-glyph refusal (A-6 downgrade, A-8 refuse)
  is proven not to cascade into whole-font or whole-run refusal.</done>
</task>

<task type="auto">
  <name>Task 3: Corpus-wide branch coverage and the two-sided D-04 refusal-rate bound</name>
  <files>tests/test_encoding_table.py</files>
  <read_first>
    02-RESEARCH.md "Every check..." table, TEXT-04 branch-coverage-vacuity warning and the
    Type1-vs-TrueType mutation row
    02-VALIDATION.md "D-04 refusal rate must be bounded two-sided" warning (an upper bound alone stays
    green if the table stops refusing anything)
    02-RESEARCH.md Section 1 "The refusal surface, precisely" table (baseline: TT-c 1.9%,
    symbolic-not-embedded 9.7%, T1-b-collapse-if-wrong +23.1%)
    tools/measure_truetype_cmap_gaps.py output (02-02, the measured TT-d/TT-e counts this bound
    incorporates)
  </read_first>
  <action>
    Write test_every_corpus_font_fires_exactly_one_branch: every font dict across the full public
    corpus resolves to exactly one branch_id, and additionally asserts branches_fired_count > 0 and
    distinct_branches_fired >= 8 (per the explicit anti-vacuity warning -- Phase 1's 0/0-pass failure
    mode, corroborated by Assumption A8's 10-distinct-combination floor). Write
    test_type1_symbolic_encoding_resolves_truetype_refuses on real corpus fonts. Write
    test_refusal_rate_within_recorded_bound: measure the actual corpus-wide refusal rate, record it,
    and assert it sits within a two-sided pinned range wide enough to include the measured TT-d/TT-e
    contribution from 02-02 and narrow enough that the Pitfall-2 regression (a jump toward 23%+)
    fails. Record the observed jump from the Type1-vs-TrueType collapse mutation as a code comment,
    mirroring this repo's "verified to reproduce" convention.
  </action>
  <acceptance_criteria>
    All three tests pass against the real corpus; the collapse mutation is demonstrated, once, to
    push the measured refusal rate outside the pinned two-sided bound.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_encoding_table.py -m corpus -q</automated>
  </verify>
  <done>D-04's refusal rate is measured, recorded, and bounded two-sided against real corpus data,
  closing the "unknown, must be measured" cost D-04 accepted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| engine.encoding_table <-> font dictionaries and embedded font programs in untrusted-shaped PDFs | Structural inspection via pikepdf plus embedded-program inspection via fontTools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-04 | Information Disclosure | branch_id/reason strings | mitigate | Reason strings are a closed set of category labels (e.g. "TT-c", "NOEMB", "ACTUALTEXT") -- never a quoted glyph name or decoded text |
| T-02-01 | Denial of Service | An unbounded or adversarial /Differences array parsed as pairwise integer/name pairs | mitigate | Parse is bounded by the array's own declared length (a PDF-internal bound, not attacker-amplifiable); the general per-document wall-clock cap lands in 02-09 as the backstop |
| T-02-08 | Denial of Service | glyph_presence()'s fontTools.ttLib/t1Lib load of an adversarial embedded font program | mitigate | Wrap the load in a try/except recording a downgrade-to-refuse verdict rather than propagating the exception, matching this repo's established "record, never crash" convention |
</threat_model>

<verification>
`pytest tests/test_encoding_table.py -q` passes, including the corpus-marked coverage and refusal-rate
tests. `uv run --frozen mypy engine/` exits 0.
</verification>

<success_criteria>
Every font in the public corpus resolves to exactly one logged branch; Type1 and TrueType's
symbolic+/Encoding ambiguity is handled distinctly, not collapsed; the D-04 refusal rate is measured
and two-sidedly bounded; per-glyph refusal never cascades to a whole font or run.
</success_criteria>

<output>
Create `.planning/phases/02-text-model/02-06-SUMMARY.md` when done
</output>
