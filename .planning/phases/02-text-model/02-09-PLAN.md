---
phase: 02-text-model
plan: 09
type: execute
wave: 7
depends_on: ["02-08"]
files_modified:
  - engine/index.py
  - tools/pdftool.py
  - tests/test_perf.py
  - tests/test_walker.py
autonomous: true
requirements: [TEXT-01]

must_haves:
  truths:
    - "D-06: pages are parsed on first access and cached; first-page latency stays under 100ms on a 100+ page document; cache eviction is LRU by glyph count, not page count, with the measured 178MB/400-page budget recorded next to the constant"
    - "TEXT-01: the interpreter, now fully wired (walk to encode to cluster to classify), walks a document in index mode and emits a run record per text run, exercised corpus-wide"
    - "Phase 2 ships a CLI (pdftool index), never a served endpoint, per the still-CLI-only constraint"
  artifacts:
    - path: "engine/index.py"
      provides: "RunIndex: page-at-a-time parse+cache, columnar per-page glyph storage, LRU-by-glyph-count eviction"
    - path: "tools/pdftool.py"
      provides: "the CLI entry point: pdftool index <file> [--page N]"
  key_links:
    - from: "engine/index.py"
      to: "engine/walker.py, engine/encoding_table.py, engine/clusterer.py, engine/classify_page.py, engine/classify_run.py"
      via: "the first place all five modules are composed into one pipeline"
      pattern: "class RunIndex"
    - from: "tools/pdftool.py"
      to: "engine/index.py"
      via: "RunIndex construction"
      pattern: "RunIndex\\("
---

<objective>
Wire the walker, encoding table, clusterer and classifier into one page-at-a-time, cached `RunIndex`
(D-06), with columnar per-page glyph storage and LRU eviction by glyph count, and ship the CLI this
phase is allowed to ship.

Purpose: every prior plan built one stage of the pipeline in isolation. This is where they compose for
the first time, and where D-06's performance discipline (measured: 21s/166MB eager on a 126-page
document vs page-at-a-time's ~15ms) actually gets enforced rather than just described.

Output: `engine/index.py`, `tools/pdftool.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-text-model/02-CONTEXT.md
@engine/walker.py
@engine/encoding_table.py
@engine/clusterer.py
@engine/classify_page.py
@engine/classify_run.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine/index.py -- RunIndex, columnar cache, LRU by glyph count</name>
  <files>engine/index.py, tests/test_perf.py</files>
  <read_first>
    02-RESEARCH.md Section 10 full section: parse-cost table, full-document-parse cost table, memory
    profile table (columnar recommendation, 3x smaller than a per-glyph dataclass), the explicit
    cache-size consequence ("cap the cache by glyph count, not page count... evict LRU")
    02-RESEARCH.md Pitfall 8 "Building the full-document run map eagerly"
    02-CONTEXT.md D-06
  </read_first>
  <behavior>
    - RunIndex(pdf_path).page(n) parses page n on first access, running the full walker to encoding
      table to clusterer to classifier pipeline, and caches the PageIndex result (run list, page
      bucket, per-run verdicts); a second .page(n) call is a cache hit, no re-parse.
    - Cache eviction is LRU by cumulative glyph count against a named constant budget -- document the
      measured 178MB/400-page-document number from Section 10 directly next to the constant.
    - Per-page glyph storage is columnar: one array.array per numeric field plus one joined,
      offset-indexed text list -- not one Python object per glyph.
  </behavior>
  <action>
    Implement RunIndex per behavior, composing engine.walker, engine.encoding_table,
    engine.clusterer and engine.classify_page/classify_run for the first time end to end.
  </action>
  <acceptance_criteria>
    RunIndex(corpus/public/irs_1040_instructions.pdf).page(0) completes in well under 100ms (the
    pinned D-06 latency test); a second .page(0) call is measurably a cache hit (near-zero, verified
    via a monkeypatched call counter on the underlying per-page parse function); a permanent
    negative-case test replaces lazy per-page indexing with an eager full-document walk up front and
    proves it blows the 100ms budget (the measured 21s figure on this same document from Section 10).
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_perf.py -q</automated>
  </verify>
  <done>The full pipeline is composed for the first time behind RunIndex, D-06's latency and caching
  discipline is enforced and proven, not just described.</done>
</task>

<task type="auto">
  <name>Task 2: tools/pdftool.py -- the CLI, still-CLI-only per constraint</name>
  <files>tools/pdftool.py</files>
  <read_first>
    tools/probe_corpus.py (the argparse CLI convention this repo already uses)
    engine/index.py (just written)
  </read_first>
  <action>
    Write a minimal argparse CLI with one subcommand, `index <pdf_path> [--page N]`: opens a
    RunIndex, and without --page prints a per-page summary table (page number, run count,
    editable/substitution/not-editable counts, page bucket) to stdout; with --page N prints that
    page's run list (run ID, display text with synthetic spaces, verdict, reason if not-editable). No
    web server, no network call, anywhere in this file -- Phase 2 is still CLI-only, no web tier
    before Gate G2b in Phase 3.
  </action>
  <acceptance_criteria>
    `python tools/pdftool.py index corpus/public/irs_form_w9.pdf` exits 0 and prints a non-empty run
    summary; the same command with `--page 0` prints at least one run.
  </acceptance_criteria>
  <verify>
    <automated>python tools/pdftool.py index corpus/public/irs_form_w9.pdf --page 0</automated>
  </verify>
  <done>Phase 2 ships a working CLI over the full pipeline, honoring the still-CLI-only constraint
  explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Corpus-wide TEXT-01 exercise through the fully-wired pipeline</name>
  <files>tests/test_walker.py</files>
  <read_first>
    tests/test_walker.py (existing test_walks_full_corpus_without_exception from 02-04, currently
    exercising only walk_page/walk_document directly, not the composed pipeline)
    harness/run_corpus_harness.py (the "record, never crash the run" convention this task follows for
    the 17 malformed and 12 encrypted documents)
  </read_first>
  <action>
    Add test_full_pipeline_walks_full_corpus_without_exception (@pytest.mark.corpus): call
    RunIndex(doc).page(n) for every page of every corpus document, not just the raw walker -- this is
    the first plan where walker, encoding table, clusterer and classifier are actually exercised
    together end to end. Record, never raise, a per-document failure reason for the malformed/
    encrypted documents where pikepdf/qpdf's repair-on-open still leaves something genuinely
    unopenable, matching the harness's established convention.
  </action>
  <acceptance_criteria>
    The full pipeline completes on all 216 public documents without an unhandled exception outside
    the documented recorded-failure path.
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_walker.py -m corpus -q</automated>
  </verify>
  <done>TEXT-01 is proven against the fully composed pipeline, corpus-wide, not just against the raw
  walker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| engine.index.RunIndex <-> whole-document processing budget | The first place a per-document resource ceiling actually needs enforcing, since this plan composes every earlier stage into one document-scale operation |
| tools/pdftool.py <-> local filesystem paths only | CLI, no network, no served endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-02-01 | Denial of Service | Decompression bomb or pathological glyph density during a full-document RunIndex walk | mitigate | Per-document glyph-count cap (Section 10: "anything above ~10M is pathological -- abort with a named reason") plus a per-document wall-clock cap, both enforced in RunIndex -- this is where the document-level budget concept, deferred from 02-04/02-05, actually lands |
| T-02-04 | Information Disclosure | tools/pdftool.py stdout prints run display text | accept | This is intentional, local-only CLI output showing a user their own document's text -- not the same boundary as the "never log document text" rule, which governs logger.debug/exception paths (PRIV-04's Phase 2 framing), not deliberate CLI stdout for a local operator with no network and no log aggregation involved |
</threat_model>

<verification>
`pytest tests/test_perf.py tests/test_walker.py -m corpus -q` passes. `python tools/pdftool.py index
corpus/public/irs_form_w9.pdf` exits 0.
</verification>

<success_criteria>
The full Phase 2 pipeline is composed behind one RunIndex API, D-06's page-at-a-time caching and
columnar storage are proven under their own performance test, and Phase 2 ships its promised CLI
without introducing any served surface.
</success_criteria>

<output>
Create `.planning/phases/02-text-model/02-09-SUMMARY.md` when done
</output>
