__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# macOS Finder metadata
.DS_Store

# Machine-local agent state. Scoped narrowly so a shared .claude/settings.json
# or .claude/skills/ can still be tracked if the project adds them.
.claude/settings.local.json
.claude/worktrees/


# Private-tier corpus bytes (D-01) - real invoices/contracts, fetched from a bucket via CI
# secrets, never committed. Only corpus/private-manifest.json (names/hashes/categories) is
# checked in.
corpus/private/
